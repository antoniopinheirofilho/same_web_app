# Environment Variables for Nexus Monorepo

# Database Path (optional)
# If not set, the database will be automatically located at the monorepo root
# Set this in production to specify where the SQLite database should be stored
# DATABASE_PATH=/path/to/your/nexus.db

# Example production paths:
# DATABASE_PATH=/var/data/nexus.db          # Linux server
# DATABASE_PATH=/opt/nexus/data/nexus.db    # Custom server location
# DATABASE_PATH=./data/nexus.db             # Relative to app directory
